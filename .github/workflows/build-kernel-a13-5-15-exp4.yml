name: Android 13 5.15 Kernel Build (EXP4)

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
    inputs:
      make_release:
        description: 'Create a GitHub release'
        required: true
        type: boolean
        default: true
      android_version:
        description: 'Android version'
        required: true
        type: string
        default: "android13"
      kernel_version:
        description: 'Kernel version'
        required: true
        type: string
        default: "5.15"
      sub_level:
        description: 'Kernel sub-level'
        required: true
        type: string
        default: "123"
      os_patch_level:
        description: 'OS patch level'
        required: true
        type: string
        default: "2023-11"
      kernelsu_variant:
        description: 'KernelSU variant'
        required: true
        type: string
        default: "Next"
      kernelsu_commit:
        description: 'KernelSU commit hash (leave blank for latest dev)'
        required: false
        type: string
        default: ""
      susfs_commit:
        description: 'SUSFS commit hash (leave blank for latest dev)'
        required: false
        type: string
        default: ""

jobs:
  build-kernel-a13-5-15:
    runs-on: ubuntu-22.04
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        include:
          - sub_level: "123"
            os_patch_level: "2023-11"
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"

    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          swap-size-mb: 8192
          remove-dotnet: "true"
          remove-android: "true"
          remove-haskell: "true"
          remove-codeql: "true"

      - name: Set CONFIG Environment Variable
        run: |
          CONFIG="${{ inputs.android_version }}-${{ inputs.kernel_version }}-${{ inputs.sub_level }}"
          echo "CONFIG=$CONFIG" >> $GITHUB_ENV
          echo "CONFIG set to: $CONFIG"

      - name: Install ccache
        run: sudo apt update && sudo apt install -y ccache

      - name: Set up ccache
        run: |
          mkdir -p ~/.cache/bazel
          ccache --version
          ccache --max-size=2G
          ccache --set-config=compression=true
          echo "CCACHE_DIR=$HOME/.ccache" >> $GITHUB_ENV

      - name: Restore ccache from cache
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ${{ inputs.android_version }}-${{ inputs.kernel_version }}-${{ inputs.sub_level }}-ccache-${{ github.sha }}
          restore-keys: |
            ${{ inputs.android_version }}-${{ inputs.kernel_version }}-${{ inputs.sub_level }}-ccache-

      - name: Cache toolchain
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: |
            kernel-build-tools
            mkbootimg
          key: toolchain-${{ runner.os }}-v1

      - name: Download toolchain
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          AOSP_MIRROR=https://android.googlesource.com
          BRANCH=main-kernel-build-2024
          git clone $AOSP_MIRROR/kernel/prebuilts/build-tools -b $BRANCH --depth 1 kernel-build-tools
          git clone $AOSP_MIRROR/platform/system/tools/mkbootimg -b $BRANCH --depth 1 mkbootimg

      - name: Set environment variables
        run: |
          echo "AVBTOOL=$GITHUB_WORKSPACE/kernel-build-tools/linux-x86/bin/avbtool" >> $GITHUB_ENV
          echo "MKBOOTIMG=$GITHUB_WORKSPACE/mkbootimg/mkbootimg.py" >> $GITHUB_ENV
          echo "UNPACK_BOOTIMG=$GITHUB_WORKSPACE/mkbootimg/unpack_bootimg.py" >> $GITHUB_ENV

      - name: Set boot sign key
        env:
          BOOT_SIGN_KEY: ${{ secrets.BOOT_SIGN_KEY }}
        run: |
          if [ ! -z "$BOOT_SIGN_KEY" ]; then
            echo "$BOOT_SIGN_KEY" > ./kernel-build-tools/linux-x86/share/avb/testkey_rsa2048.pem
            echo "BOOT_SIGN_KEY_PATH=$GITHUB_WORKSPACE/kernel-build-tools/linux-x86/share/avb/testkey_rsa2048.pem" >> $GITHUB_ENV
          else
            echo "BOOT_SIGN_KEY is not set. Exiting..."
            exit 1
          fi

      - name: Install Repo
        run: |
          mkdir -p ./git-repo
          curl https://storage.googleapis.com/git-repo-downloads/repo > ./git-repo/repo
          chmod a+rx ./git-repo/repo
          echo "REPO=$GITHUB_WORKSPACE/./git-repo/repo" >> $GITHUB_ENV

      - name: Clone AnyKernel3
        run: |
          mkdir -p "$CONFIG/AnyKernel3"
          git clone https://github.com/WildPlusKernel/AnyKernel3.git -b "gki-2.0" "$CONFIG/AnyKernel3"

      - name: Initialize and Sync Kernel Source
        run: |
          mkdir -p "$CONFIG"
          cd "$CONFIG"
          FORMATTED_BRANCH="${{ inputs.android_version }}-${{ inputs.kernel_version }}-${{ inputs.os_patch_level }}"
          $REPO init --depth=1 --u https://android.googlesource.com/kernel/manifest -b common-${FORMATTED_BRANCH} --repo-rev=v2.16
          REMOTE_BRANCH=$(git ls-remote https://android.googlesource.com/kernel/common ${FORMATTED_BRANCH})
          DEFAULT_MANIFEST_PATH=.repo/manifests/default.xml
          if grep -q deprecated <<< $REMOTE_BRANCH; then
            echo "Found deprecated branch: $FORMATTED_BRANCH"
            sed -i "s/\"${FORMATTED_BRANCH}\"/\"deprecated\/${FORMATTED_BRANCH}\"/g" $DEFAULT_MANIFEST_PATH
          fi
          $REPO --version
          $REPO --trace sync -c -j$(nproc --all) --no-tags --fail-fast

      - name: Add KernelSU and SUSFS
        run: |
          cd "$CONFIG"
          if [ -z "${{ inputs.kernelsu_commit }}" ]; then
            echo "Using latest KernelSU Next dev build..."
            curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -s next
            KSU_COMMIT=$(git -C KernelSU-Next rev-parse HEAD)
          else
            echo "Downloading KernelSU from commit: ${{ inputs.kernelsu_commit }}"
            curl -LSs -o KernelSU-Next.zip "https://github.com/KernelSU-Next/KernelSU-Next/archive/${{ inputs.kernelsu_commit }}.zip"
            unzip -o KernelSU-Next.zip -d .
            mv KernelSU-Next-${{ inputs.kernelsu_commit }} KernelSU-Next
            KSU_COMMIT=${{ inputs.kernelsu_commit }}
          fi
          if [ -z "${{ inputs.susfs_commit }}" ]; then
            echo "Using latest SUSFS dev build..."
            SUSFS_URL="https://gitlab.com/simonpunk/susfs4ksu/-/archive/master/susfs4ksu-master.zip"
            curl -LSs -o susfs4ksu.zip "$SUSFS_URL"
            unzip -o susfs4ksu.zip -d .
            mv susfs4ksu-master susfs4ksu
            # Fallback to timestamp-based identifier since ZIP lacks Git history
            SUSFS_COMMIT="master-$(date +%s)"
          else
            echo "Downloading SUSFS from commit: ${{ inputs.susfs_commit }}"
            curl -LSs -o susfs4ksu.zip "https://gitlab.com/simonpunk/susfs4ksu/-/archive/${{ inputs.susfs_commit }}/susfs4ksu-${{ inputs.susfs_commit }}.zip"
            unzip -o susfs4ksu.zip -d .
            mv susfs4ksu-${{ inputs.susfs_commit }} susfs4ksu
            SUSFS_COMMIT=${{ inputs.susfs_commit }}
          fi
          echo "KSU_COMMIT=$KSU_COMMIT" >> $GITHUB_ENV
          echo "SUSFS_COMMIT=$SUSFS_COMMIT" >> $GITHUB_ENV

      - name: Apply Patches (only if commits not specified)
        if: inputs.kernelsu_commit == '' || inputs.susfs_commit == ''
        run: |
          cd "$CONFIG"
          if [ -z "${{ inputs.kernelsu_commit }}" ]; then
            echo "Applying KernelSU patches..."
            cd KernelSU-Next
            cp ../susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch ./
            patch -p1 --forward --fuzz=3 < 10_enable_susfs_for_ksu.patch || true
            cp ../../kernel_patches/next/susfs_fix_patches/v1.5.9/fix_apk_sign.c.patch ./
            patch -p1 --forward --fuzz=3 < fix_apk_sign.c.patch
            cp ../../kernel_patches/next/susfs_fix_patches/v1.5.9/fix_core_hook.c.patch ./
            patch -p1 --forward --fuzz=3 < fix_core_hook.c.patch
            cp ../../kernel_patches/next/susfs_fix_patches/v1.5.9/fix_selinux.c.patch ./
            patch -p1 --forward --fuzz=3 < fix_selinux.c.patch
            cp ../../kernel_patches/next/susfs_fix_patches/v1.5.9/fix_ksud.c.patch ./
            patch -p1 --forward --fuzz=3 < fix_ksud.c.patch
            cp ../../kernel_patches/next/susfs_fix_patches/v1.5.9/fix_rules.c.patch ./
            patch -p1 --forward --fuzz=3 < fix_rules.c.patch
            cp ../../kernel_patches/next/susfs_fix_patches/v1.5.9/fix_sucompat.c.patch ./
            patch -p1 --forward --fuzz=3 < fix_sucompat.c.patch
            cp ../../kernel_patches/next/susfs_fix_patches/v1.5.9/fix_kernel_compat.c.patch ./
            patch -p1 --forward --fuzz=3 < fix_kernel_compat.c.patch
            cd ..
          fi
          if [ -z "${{ inputs.susfs_commit }}" ]; then
            echo "Applying SUSFS patches..."
            cp susfs4ksu/kernel_patches/50_add_susfs_in_gki-${{ inputs.android_version }}-${{ inputs.kernel_version }}.patch ./common/
            cd common
            patch -p1 --forward --fuzz=3 < 50_add_susfs_in_gki-${{ inputs.android_version }}-${{ inputs.kernel_version }}.patch
            cd ..
            cp susfs4ksu/kernel_patches/fs/* ./common/fs/
            cp susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/
          fi

      - name: Apply Hooks and Hide Stuff Patches
        run: |
          cd "$CONFIG/common"
          cp ../../kernel_patches/next/scope_min_manual_hooks_v1.4.patch ./
          patch -p1 -F 3 < scope_min_manual_hooks_v1.4.patch
          cp ../../kernel_patches/69_hide_stuff.patch ./
          patch -p1 -F 3 < 69_hide_stuff.patch

      - name: Add Configuration Settings
        run: |
          cd "$CONFIG"
          echo "CONFIG_KSU=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_KPROBES_HOOK=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_SU=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TMPFS_XATTR=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TMPFS_POSIX_ACL=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_IP_NF_TARGET_TTL=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_IP6_NF_TARGET_HL=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_IP6_NF_MATCH_HL=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_CPU_FREQ_GOV_WALT=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TCP_CONG_ADVANCED=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TCP_CONG_BBR=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_NET_SCH_FQ=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TCP_CONG_BIC=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TCP_CONG_WESTWOOD=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TCP_CONG_HTCP=n" >> ./common/arch/arm64/configs/gki_defconfig
          sed -i 's/check_defconfig//' ./common/build.config.gki

      - name: Config Kernel Name
        run: |
          cd "$CONFIG"
          perl -pi -e 's|echo "\$res"|echo "-android13-8-00008-g3ca6a2912c7e-ab11087001"|' ./common/scripts/setlocalversion
          perl -pi -e 's{UTS_VERSION="\$\(echo \$UTS_VERSION \$CONFIG_FLAGS \$TIMESTAMP \| cut -b -\$UTS_LEN\)"}{UTS_VERSION="#1 SMP PREEMPT Sat Nov 11 06:58:10 UTC 2023"}' ./common/scripts/mkcompile_h

      - name: Build with retry
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 30
          max_attempts: 3
          retry_on: timeout
          command: |
            set -e
            set -x
            cd "$CONFIG"
            if [ -f "build/build.sh" ]; then
              LTO=thin BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh CC="/usr/bin/ccache clang" || exit 1
            else
              tools/bazel build --disk_cache=/home/runner/.cache/bazel --config=fast --lto=thin //common:kernel_aarch64_dist || exit 1
            fi
            ccache --show-stats

      - name: Create Boot Images and ZIP Files
        run: |
          echo "===== Starting Boot Image and ZIP Creation ====="
          cd "$CONFIG"
          mkdir -p bootimgs
          echo "Copying kernel images..."
          cp -v ./out/${{ inputs.android_version }}-${{ inputs.kernel_version }}/dist/boot.img ./bootimgs/ || { echo "ERROR: boot.img missing in dist/"; exit 1; }
          [ -f ./bootimgs/Image ] || cp -v ./out/${{ inputs.android_version }}-${{ inputs.kernel_version }}/common/arch/arm64/boot/Image ./bootimgs/ || { echo "ERROR: Image missing everywhere"; exit 1; }
          echo "Preparing AnyKernel3 files..."
          cp -v ./bootimgs/Image ./AnyKernel3/Image || { echo "ERROR: Failed to copy Image"; exit 1; }
          echo "Creating ZIP archive..."
          mkdir -p release
          cd AnyKernel3
          KSUVER=$(cat ../KernelSU-Next/version 2>/dev/null || echo "v0.0.0")
          KSU_SUBVER=$(echo $KSUVER | grep -oP '(?<=v\d+\.\d+\.\d+\.)\d{5}' || echo "00000")
          SUSFSVER=$(cat ../susfs4ksu/version 2>/dev/null || echo "v1.5.9")
          echo "Using KernelSU version: $KSUVER (Sub-version: $KSU_SUBVER), SUSFS version: $SUSFSVER"
          zip -r9 "../release/${{ inputs.kernelsu_variant }}-$KSUVER-$KSU_SUBVER-$SUSFSVER-${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-AnyKernel3.zip" ./*
          echo "Final artifacts:"
          ls -lh ../release/
          [ "$(ls -A ../release/)" ] || { echo "ERROR: No ZIP files created!"; exit 1; }
          echo "===== Boot Image and ZIP Creation Complete ====="

      - name: Generate Release Notes
        run: |
          cd "$CONFIG"
          KSUVER=$(cat KernelSU-Next/version 2>/dev/null || echo "v0.0.0")
          KSU_SUBVER=$(echo $KSUVER | grep -oP '(?<=v\d+\.\d+\.\d+\.)\d{5}' || echo "00000")
          SUSFSVER=$(cat susfs4ksu/version 2>/dev/null || echo "v1.5.9")
          echo "RELEASE_NOTES<<EOF" >> $GITHUB_ENV
          echo "This release contains KernelSU-$KSUVER-$KSU_SUBVER (Dev) and SUSFS $SUSFSVER" >> $GITHUB_ENV
          echo "" >> $GITHUB_ENV
          echo "Features:" >> $GITHUB_ENV
          echo "- KernelSU-Next-Dev $KSUVER-$KSU_SUBVER" >> $GITHUB_ENV
          echo "- SUSFS $SUSFSVER" >> $GITHUB_ENV
          echo "- Manual Hooks" >> $GITHUB_ENV
          echo "- Wireguard Support" >> $GITHUB_ENV
          echo "" >> $GITHUB_ENV
          echo "Notes:" >> $GITHUB_ENV
          echo "This is a MODIFIED kernel only meant for VERMEER devices a.k.a Redmi K70 / Poco F6 Pro, running on HyperOS 1.0.6." >> $GITHUB_ENV
          echo "If you do not have this device please do not flash this." >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Android 13/14/15 boot image build script
        run: |
          cd "$CONFIG/bootimgs"
          echo "Building boot.img"
          $MKBOOTIMG --header_version 4 --kernel Image --output boot.img
          $AVBTOOL add_hash_footer --partition_name boot --partition_size $((64 * 1024 * 1024)) --image boot.img --algorithm SHA256_RSA2048 --key $BOOT_SIGN_KEY_PATH
          cp ./boot.img "../${{ inputs.kernelsu_variant }}-$KSUVER-${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}-boot.img"

      - name: Create Release
        if: ${{ inputs.make_release }}
        id: create_release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: ${{ env.CONFIG }}/release/*.zip
          tag_name: ${{ inputs.kernelsu_variant }}-${{ env.KSUVER }}-${{ env.KSU_SUBVER }}-${{ env.SUSFSVER }}-${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}
          name: Release ${{ inputs.kernelsu_variant }}-${{ env.KSUVER }}-${{ env.KSU_SUBVER }}-${{ env.SUSFSVER }}-${{ inputs.android_version }}-${{ inputs.kernel_version }}.${{ inputs.sub_level }}-${{ inputs.os_patch_level }}
          body: ${{ env.RELEASE_NOTES }}
          draft: false
          prerelease: true

      - name: Upload Build Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kernel-${{ env.CONFIG }}
          path: |
            ${{ env.CONFIG }}/release/*.zip
            ${{ env.CONFIG }}/bootimgs/*.img
            ${{ env.CONFIG }}/*-boot.img
            ./out/${{ inputs.android_version }}-${{ inputs.kernel_version }}/dist/*.img
          if-no-files-found: warn
          compression-level: 6
          overwrite: false
          include-hidden-files: false

      - name: Check for .rej files
        if: failure()
        run: |
          echo "::group::Checking for .rej files..."
          REJ_FILES=$(find . -name '*.rej')
          if [ -z "$REJ_FILES" ]; then
            echo "No .rej files found."
          else
            echo "Found .rej files:"
            echo "$REJ_FILES"
            for f in $REJ_FILES; do
              echo "--- $f ---"
              cat "$f"
            done
          fi
          echo "::endgroup::"

      - name: Upload .rej files as artifact
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: patch-rejects
          path: "**/*.rej"
          if-no-files-found: warn
          compression-level: 6
          overwrite: false
          include-hidden-files: false
